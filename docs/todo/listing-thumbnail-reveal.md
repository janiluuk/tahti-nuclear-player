# Listing thumbnail ImageReveal sweep

Listing covers (rows, widget cards, directory tiles) use Storybook
`ImageReveal`. Playable row thumbs use `MediaArtwork`, which already
wraps `ImageReveal` except at `sm`.

**Leave:** upload/edit pickers, channel designer, entity heroes, fullscreen
player, map atlas, comment avatars, video/iframe backdrops.

Details and file list: `packages/tahti-web/UI-REDESIGN-WORKLOG.md`
(2026-09-04 — Listing thumbnail ImageReveal sweep).
